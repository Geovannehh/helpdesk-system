import {FormEvent,useState} from 'react';import {api} from '../lib/api';import {useNavigate} from 'react-router-dom';export default function Login(){const [email,setEmail]=useState('admin@helpdesk.local'),[password,setPassword]=useState('Admin@123'),[error,setError]=useState('');const nav=useNavigate();async function submit(e:FormEvent){e.preventDefault();try{const {data}=await api.post('/auth/login',{email,password});localStorage.setItem('token',data.token);localStorage.setItem('user',JSON.stringify(data.user));nav('/')}catch{setError('E-mail ou senha inválidos')}}return <div className="min-h-screen grid place-items-center bg-gradient-to-br from-purple-700 to-indigo-950 p-5"><form onSubmit={submit} className="bg-white rounded-3xl p-8 w-full max-w-md shadow-2xl"><div className="text-3xl font-black text-brand">HelpDesk<span className="text-black">+</span></div><p className="text-gray-500 mt-2 mb-8">Gestão de suporte técnico</p>{error&&<div className="bg-red-50 text-red-600 p-3 rounded-xl mb-4">{error}</div>}<label className="text-sm font-semibold">E-mail<input className="w-full mt-1 mb-4 p-3 border rounded-xl" value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="text-sm font-semibold">Senha<input type="password" className="w-full mt-1 mb-6 p-3 border rounded-xl" value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="w-full bg-brand text-white p-3 rounded-xl font-bold">Entrar</button><p className="text-xs text-gray-400 mt-5">Admin inicial: admin@helpdesk.local / Admin@123</p></form></div>}
