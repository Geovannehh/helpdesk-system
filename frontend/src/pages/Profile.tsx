import {useState} from 'react';import {api} from '../lib/api';import {getUser} from '../lib/auth';export default function Profile(){const u=getUser()!;const [name,setName]=useState(u.name),[phone,setPhone]=useState(''),[password,setPassword]=useState('');async function save(e:any){e.preventDefault();const r=await api.put('/users/me',{name,phone,password:password||undefined});localStorage.setItem('user',JSON.stringify(r.data));setPassword('');alert('Perfil atualizado.')}return <div className="max-w-xl"><h2 className="text-3xl font-black mb-6">Meu perfil</h2><form onSubmit={save} className="bg-white border rounded-2xl p-6 space-y-4"><input className="w-full p-3 border rounded-xl" value={name} onChange={e=>setName(e.target.value)} placeholder="Nome"/><input className="w-full p-3 border rounded-xl" value={phone} onChange={e=>setPhone(e.target.value)} placeholder="Telefone"/><input type="password" className="w-full p-3 border rounded-xl" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Nova senha"/><button className="bg-brand text-white p-3 rounded-xl w-full">Salvar alterações</button></form></div>}
