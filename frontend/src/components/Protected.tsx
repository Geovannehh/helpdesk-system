import {Navigate} from 'react-router-dom';import {getUser} from '../lib/auth';export default function Protected({children}:{children:React.ReactNode}){return getUser()?children:<Navigate to="/login" replace/>}
